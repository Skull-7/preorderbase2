import 'package:flutter/material.dart';
import 'package:preorderbase2/mainscreens/menu_page.dart';

class checkout extends StatelessWidget{
  //List food;
  //var food2=menu_pageState().foodItems;
  //List food3=

  //checkout({required this.food});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      /*body: ListView.builder(
        itemBuilder: (context, index){
          return food2[index].count!=0?Container(
            child:Text("${food2[index].name}"),
          ):Center(
            child: Text("Cart is Empty"),
          );
        },
        itemCount: food2.length,
      ),*/
    );
  }
}